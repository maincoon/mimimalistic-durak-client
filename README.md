# Minimalistic Durak Client (MVP)

Минималистичный Web SPA клиент для карточной игры «Дурак» (подкидной, 2 игрока), разработанный на React + TypeScript. Клиент взаимодействует с серверным API `globo.games` по протоколу WebSocket (XML).

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Генерация ассетов (карт)

Перед первым запуском необходимо сгенерировать SVG-файлы игровых карт:

```bash
# Сборка инструмента
npm -w tools/asset-generator run build

# Генерация ассетов в apps/durak-client/public/cards
npm -w tools/asset-generator run generate
```

### 3. Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: <http://localhost:5173>

### 4. Сборка для продакшена

```bash
npm run build
```

Результат сборки находится в `apps/durak-client/dist`.

### 5. Запуск тестов

```bash
npm test
```

## 📚 Документация

Проект подробно задокументирован в директории `docs/`:

- **[Главная документация клиента (MVP)](docs/mimimalistic-durak-client.md)** — архитектура, сущности, флоу, roadmap.
- [Авторизация клиент-сервер](docs/авторизация-клиент-сервер.md)
- [Жизненные циклы](docs/жизненные-циклы.md)
- [Описание игры «Дурак»](docs/описание-игры-дурак.md)
- [Протокол игры «Дурак»](docs/протокол-игры-дурак.md)
- [Протокол клиент-сервер](docs/протокол-клиент-сервер.md)
- [Состояния каналов](docs/состояния-каналов.md)

## 🏗 Структура проекта

Проект организован как Monorepo (npm workspaces):

- **`apps/durak-client`**: React-приложение (клиент).
- **`packages/core`**: Ядро логики (сессия, матчинг, стейт-машина).
- **`packages/durak`**: Специфичная логика игры «Дурак».
- **`packages/protocol`**: Работа с XML-протоколом (парсинг, команды).
- **`packages/transport`**: WebSocket транспорт.
- **`tools/asset-generator`**: Утилита для генерации SVG-графики.

## 🛠 Технологии

- **Language:** TypeScript
- **UI:** React 19, Vite
- **Test:** Vitest, JSDOM
- **Transport:** Native WebSocket, XML Protocol
